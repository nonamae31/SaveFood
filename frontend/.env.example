# SaveFood Frontend — Environment Variables
# Copy file này thành .env.local và điền giá trị thực tế
# File .env.local đã được .gitignore — KHÔNG commit file .env.local lên Git

# URL API Backend (thay đổi theo môi trường)
VITE_API_BASE_URL=http://localhost:5101/api
